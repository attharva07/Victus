# Development: backend API served by uvicorn on localhost:8000
VITE_API_URL=http://127.0.0.1:8000

# Production note:
# When UI and API are served from the same host, set VITE_API_URL to "/api"
# only if API routes are namespaced under /api.
